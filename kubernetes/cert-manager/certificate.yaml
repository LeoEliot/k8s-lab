apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: wildcard-production
  namespace: kube-system
spec:
  secretName: wildcard-production
  issuerRef:
    name: ca-issuer
    kind: ClusterIssuer
  commonName: "malef.lan"
  dnsNames:
    - "malef.lan"
    - "*.malef.lan"
