fullnameOverride: "victoria-logs"

server:
  enabled: true
  priorityClassName: ""
  retentionPeriod: 7d

  persistentVolume:
    enabled: true
    accessModes:
      - ReadWriteOnce
    size: 50Gi

  resources: {}
      # limits:
      #   cpu: 500m
      #   memory: 512Mi
      # requests:
      #   cpu: 500m
      #   memory: 512Mi

  vmServiceScrape:
    enabled: true

  route:
    enabled: true
    hostnames:
      - victoria-logs.malef.lan
    parentRefs:
      - name: envoy-internal
        namespace: network
    extraRules:
      - filters:
          - type: RequestRedirect
            requestRedirect:
              path:
                type: ReplaceFullPath
                replaceFullPath: /select/vmui/
              statusCode: 302
        matches:
          - path:
              type: Exact
              value: /

vector:
  enabled: true
  resources: {}

dashboards:
  enabled: true
  grafanaOperator:
    enabled: false

